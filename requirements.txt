# Core NLP & ML
torch>=2.0
transformers>=4.30
sentence-transformers>=2.2
scikit-learn>=1.3

# Vietnamese NLP
underthesea

# Data processing
pandas>=2.0
numpy>=1.24
pyarrow
fastparquet

# Visualization
matplotlib>=3.7
seaborn>=0.12

# PDF extraction (Docling supports OCR + no-OCR modes for the demo flow)
docling>=2.0

# Statistics
scipy>=1.11

# Hyperparameter tuning
optuna>=3.0

# Config + env
pyyaml
python-dotenv

# LLM labeling (Gemini)
google-genai

datasets
accelerate
sentencepiece

# QLoRA / PEFT — LLM bake-off (Phase 02b, model.type=decoder_lora)
peft>=0.11
# bitsandbytes: cần build hỗ trợ CUDA của máy. RTX 5070 Ti = Blackwell sm_120 + torch cu13x
# -> verify `import bitsandbytes` chạy được lúc local; nếu chưa có wheel cu13x, xem fallback trong config/train_llm.yml
bitsandbytes>=0.43