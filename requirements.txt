# Core NLP & ML
torch>=2.0
transformers>=4.30
sentence-transformers>=2.2
scikit-learn>=1.3

# Vietnamese NLP
underthesea                 # sent_tokenize (tach cau)
py_vncorenlp                # RDRSegmenter cho PhoBERT (word_segment) — can Java/JDK

# Data processing
pandas>=2.0
numpy>=1.24
pyarrow
fastparquet

# Visualization
matplotlib>=3.7
seaborn>=0.12

# PDF extraction (Docling supports OCR + no-OCR modes for the demo flow)
docling>=2.0

# Statistics
scipy>=1.11

# Hyperparameter tuning
optuna>=3.0

# Noise filtering translate-train (spec 01 #4)
cleanlab>=2.5

# Config + env
pyyaml
python-dotenv

# Luu/upload model (safetensors + HF Hub)
safetensors
huggingface_hub

# LLM labeling (Gemini)
google-genai

datasets
accelerate
sentencepiece

semantic-text-splitter